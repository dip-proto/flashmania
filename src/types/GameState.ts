export enum GameState {
  MENU = 'menu',
  READY = 'ready',
  RACING = 'racing',
  PAUSED = 'paused',
  FINISHED = 'finished',
}

export enum MedalType {
  BRONZE = 'bronze',
  SILVER = 'silver',
  GOLD = 'gold',
  AUTHOR = 'author',
}
